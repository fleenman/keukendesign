{
  "name": "keukendesign-static-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run generate:content && nuxt dev --host 127.0.0.1 --port 3000",
    "generate:content": "node scripts/generate-content.mjs",
    "generate": "nuxt generate",
    "preview:static": "npx serve .output/public",
    "audit:content": "node scripts/audit-content.mjs",
    "sitemap": "node scripts/generate-sitemap.mjs",
    "build": "npm run generate:content && npm run audit:content && npm run sitemap && nuxt generate"
  },
  "dependencies": {
    "@nuxt/kit": "^3.17.0",
    "nuxt": "^3.17.0",
    "vue": "^3.5.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "serve": "^14.2.0",
    "typescript": "^5.8.0"
  }
}
